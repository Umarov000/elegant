import React from "react";
import Newsletter from "../../components/newsletter/Newsletter";

const ProductDetails = () => {
  return (
    <div>
      ProductDetails-ABdulbosit
      <Newsletter />
    </div>
  );
};

export default React.memo(ProductDetails);
